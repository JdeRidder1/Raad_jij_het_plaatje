class Canvas {
    private readonly canvas: HTMLCanvasElement; // find the right type
    private readonly ctx: CanvasRenderingContext2D; // find the right type

    public constructor() {

    }
    public writeTextToCanvas(text: string, fontSize: number, xCoordinate: number, yCoordinate:number, color:string, alignment: any): void {

    }
    public writeImageToCanvas(src: string, xCoordinate: number, yCoordinate: number, deltaX:number , deltaY: number, ): void {

    }
    public writeButtonToCanvas() {

    }
}